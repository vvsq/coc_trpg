# LLM 供应商配置（复制本文件为 .env 后填写；密钥仅存本地，不入 git）
# 留空 LLM_API_KEY 即视为未配置，系统自动回退纯人工主持。
# 4.1+：KP 控制台「LLM 设置」面板可在线改写本文件并热重载，无需重启。

# ── 供应商预设（ccswitch 式：认准一家，抄 base_url 与 model 即可）──────────
# 模型名迭代快，静态表只作推荐（2026-09-06 按官方文档核实）；真实可用列表
# 以 KP 设置面板「探测模型」（GET /llm/models，client.models.list()）为准，可手填兜底。
# 智谱 GLM        base_url=https://open.bigmodel.cn/api/paas/v4      model=glm-5.3-flash / glm-5.3
# DeepSeek       base_url=https://api.deepseek.com/v1               model=deepseek-v4-pro / deepseek-v4-flash
# 阿里云百炼千问  base_url=https://dashscope.aliyuncs.com/compatible-mode/v1  model=qwen-flash / qwen-plus / qwen-max
# OpenAI         base_url=https://api.openai.com/v1                 model=gpt-5-mini / gpt-5.2
# 其他 OpenAI 兼容中转：直接填其中转地址即可（会识别为 custom）

LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=
LLM_MODEL=qwen-flash

# 演示/断网兜底（4.1+ 可选项）：model 填 mock 即启用 MockLLMClient，
# 不发真实网络请求，AI 返回固定演示建议，无 key 可跑通全流程 UI。
# LLM_MODEL=mock

# 可选：请求超时秒数（默认 30）与失败重试次数（默认 2）
# LLM_TIMEOUT=30
# LLM_RETRIES=2
